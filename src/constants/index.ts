const APIUrl = import.meta.env.VITE_API_URL as string;
const UploadUrl = import.meta.env.VITE_UPLOAD_URL as string;
const FileUrl = import.meta.env.VITE_FILE_URL as string;

export {APIUrl, UploadUrl, FileUrl}
